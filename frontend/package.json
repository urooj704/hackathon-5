{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "description": "Vercel wrapper when Root Directory is `frontend` — builds web-form and copies .next here. Includes `next` so Vercel can detect the framework.",
  "scripts": {
    "build": "npm run build --prefix web-form && node sync-next-output.js"
  },
  "dependencies": {
    "next": "14.2.35",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "three": "^0.183.2"
  }
}
