{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "description": "Vercel wrapper when Root Directory is `frontend` — builds web-form and copies .next here.",
  "scripts": {
    "build": "npm run build --prefix web-form && node sync-next-output.js"
  }
}
